import { summary } from './summary'

/**
 * Dashboard router
 * Aggregated stats and activity for the coach dashboard
 *
 * Procedures:
 * - summary: Get dashboard summary with athlete counts, program stats, and recent activity
 */
export const dashboard = {
  summary,
}
